# built-mitm-proxy-deployment-we — Docker Compose
# Generated by Aether Infra Agent on 2026-06-09T16:02:59.568Z

version: '3.9'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: built-mitm-proxy-deployment-we:latest
    container_name: built_mitm_proxy_deployment_we_app
    ports:
      - "${PORT:-3001}:3001"
    environment:
      - NODE_ENV=${NODE_ENV:-production}
      - PORT=3001
    restart: unless-stopped
    networks:
      - built_mitm_proxy_deployment_we_net
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:3001/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Uncomment for PostgreSQL support
  # db:
  #   image: postgres:16-alpine
  #   container_name: built_mitm_proxy_deployment_we_db
  #   environment:
  #     POSTGRES_DB: built_mitm_proxy_deployment_we
  #     POSTGRES_USER: ${DB_USER:-admin}
  #     POSTGRES_PASSWORD: ${DB_PASSWORD:-changeme}
  #   volumes:
  #     - db_data:/var/lib/postgresql/data
  #   networks:
  #     - built_mitm_proxy_deployment_we_net

networks:
  built_mitm_proxy_deployment_we_net:
    driver: bridge

# volumes:
#   db_data:
