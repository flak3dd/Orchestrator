# build-mitm-deployment-platform — Docker Compose
# Generated by Aether Infra Agent on 2026-06-09T16:33:32.925Z

version: '3.9'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: build-mitm-deployment-platform:latest
    container_name: build_mitm_deployment_platform_app
    ports:
      - "${PORT:-3001}:3001"
    environment:
      - NODE_ENV=${NODE_ENV:-production}
      - PORT=3001
    restart: unless-stopped
    networks:
      - build_mitm_deployment_platform_net
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:3001/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Uncomment for PostgreSQL support
  # db:
  #   image: postgres:16-alpine
  #   container_name: build_mitm_deployment_platform_db
  #   environment:
  #     POSTGRES_DB: build_mitm_deployment_platform
  #     POSTGRES_USER: ${DB_USER:-admin}
  #     POSTGRES_PASSWORD: ${DB_PASSWORD:-changeme}
  #   volumes:
  #     - db_data:/var/lib/postgresql/data
  #   networks:
  #     - build_mitm_deployment_platform_net

networks:
  build_mitm_deployment_platform_net:
    driver: bridge

# volumes:
#   db_data:
