# synaptiq-dashboard-with-user-l — Docker Compose
# Generated by Aether Infra Agent on 2026-06-09T16:30:51.433Z

version: '3.9'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: synaptiq-dashboard-with-user-l:latest
    container_name: synaptiq_dashboard_with_user_l_app
    ports:
      - "${PORT:-3001}:3001"
    environment:
      - NODE_ENV=${NODE_ENV:-production}
      - PORT=3001
    restart: unless-stopped
    networks:
      - synaptiq_dashboard_with_user_l_net
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:3001/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Uncomment for PostgreSQL support
  # db:
  #   image: postgres:16-alpine
  #   container_name: synaptiq_dashboard_with_user_l_db
  #   environment:
  #     POSTGRES_DB: synaptiq_dashboard_with_user_l
  #     POSTGRES_USER: ${DB_USER:-admin}
  #     POSTGRES_PASSWORD: ${DB_PASSWORD:-changeme}
  #   volumes:
  #     - db_data:/var/lib/postgresql/data
  #   networks:
  #     - synaptiq_dashboard_with_user_l_net

networks:
  synaptiq_dashboard_with_user_l_net:
    driver: bridge

# volumes:
#   db_data:
