# build-web-app-to-deploy-mitm-c — Docker Compose
# Generated by Aether Infra Agent on 2026-06-09T16:25:59.544Z

version: '3.9'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: build-web-app-to-deploy-mitm-c:latest
    container_name: build_web_app_to_deploy_mitm_c_app
    ports:
      - "${PORT:-3001}:3001"
    environment:
      - NODE_ENV=${NODE_ENV:-production}
      - PORT=3001
    restart: unless-stopped
    networks:
      - build_web_app_to_deploy_mitm_c_net
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:3001/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Uncomment for PostgreSQL support
  # db:
  #   image: postgres:16-alpine
  #   container_name: build_web_app_to_deploy_mitm_c_db
  #   environment:
  #     POSTGRES_DB: build_web_app_to_deploy_mitm_c
  #     POSTGRES_USER: ${DB_USER:-admin}
  #     POSTGRES_PASSWORD: ${DB_PASSWORD:-changeme}
  #   volumes:
  #     - db_data:/var/lib/postgresql/data
  #   networks:
  #     - build_web_app_to_deploy_mitm_c_net

networks:
  build_web_app_to_deploy_mitm_c_net:
    driver: bridge

# volumes:
#   db_data:
