# you-are-shadowgrok-you-are-an- — Docker Compose
# Generated by Aether Infra Agent on 2026-06-09T16:07:44.264Z

version: '3.9'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: you-are-shadowgrok-you-are-an-:latest
    container_name: you_are_shadowgrok_you_are_an__app
    ports:
      - "${PORT:-3001}:3001"
    environment:
      - NODE_ENV=${NODE_ENV:-production}
      - PORT=3001
    restart: unless-stopped
    networks:
      - you_are_shadowgrok_you_are_an__net
    healthcheck:
      test: ["CMD", "curl", "-sf", "http://localhost:3001/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Uncomment for PostgreSQL support
  # db:
  #   image: postgres:16-alpine
  #   container_name: you_are_shadowgrok_you_are_an__db
  #   environment:
  #     POSTGRES_DB: you_are_shadowgrok_you_are_an_
  #     POSTGRES_USER: ${DB_USER:-admin}
  #     POSTGRES_PASSWORD: ${DB_PASSWORD:-changeme}
  #   volumes:
  #     - db_data:/var/lib/postgresql/data
  #   networks:
  #     - you_are_shadowgrok_you_are_an__net

networks:
  you_are_shadowgrok_you_are_an__net:
    driver: bridge

# volumes:
#   db_data:
