# synaptiq-dashboard-with-user-l — GitHub Actions CI/CD
# Generated by Aether Infra Agent on 2026-06-09T16:30:51.433Z

name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json
      - run: cd backend && npm ci
      - run: cd backend && npm test --if-present

  build:
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4
      - name: Build Docker image
        run: docker build -t synaptiq-dashboard-with-user-l:${{ github.sha }} .
      - name: Tag as latest
        run: docker tag synaptiq-dashboard-with-user-l:${{ github.sha }} synaptiq-dashboard-with-user-l:latest
